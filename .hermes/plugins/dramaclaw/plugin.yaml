name: dramaclaw
version: 0.1.0
description: "DramaClaw API tools for 虾导. Uses direct HTTP client calls with the injected agent token; no shell, subprocess, or curl."
author: "DramaClaw"
kind: backend
